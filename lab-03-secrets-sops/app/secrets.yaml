# Kubernetes Secret - CLEARTEXT (encrypt me with SOPS!)
#
# WARNING: This file contains sensitive data in plaintext!
# Run: ../scripts/encrypt-in-place.sh to secure it.
#
apiVersion: v1
kind: Secret
metadata:
  name: app-db-credentials
  namespace: default
  labels:
    app: secrets-demo
type: Opaque
stringData:
  DB_PASSWORD: "Workshop-Secret-2026!"
  DB_USER: "app_user"
  DB_HOST: "postgres.database.local"
